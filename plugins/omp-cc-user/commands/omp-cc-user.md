---
description: Manage personal ~/.claude resources (skills, plugins, commands) and their omp visibility
---

Run `python3 skill://omp-cc-user/scripts/omp_cc_user.py $ARGUMENTS` in bash (on Windows, fall back to `python` or `py -3` if `python3` is missing) and relay the script output verbatim to the user. Do not interpret, reformat, or add commentary.
